apiVersion: v1
kind: Service
metadata:
  name: kubecounter-client
  namespace: kubecounter
spec:
  selector:
    app: kubecounter-client
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30173  # optional, maps to http://<node>:30173
---