apiVersion: v1
kind: Service
metadata:
  name: kubecounter-client-svc
  namespace: kubecounter
  labels:
    app.kubernetes.io/name: kubecounter
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP

  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http-web

  selector:
    app.kubernetes.io/name: kubecounter
    app.kubernetes.io/component: frontend